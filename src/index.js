import React from 'react';
import ReactDOM from 'react-dom/client';
import ProductClassifier from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <ProductClassifier />
  </React.StrictMode>
);
